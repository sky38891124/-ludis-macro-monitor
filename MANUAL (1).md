# Ludis Macro Daily Monitor — 사용 설명서

매일 아침 자동으로 생성되는 매크로 리포트를 **읽고, 고치고, 늘리는** 방법.
사전 지식 없이 순서대로 따라올 수 있도록 썼다.

---

## 목차

1. [이 도구가 하는 일](#1-이-도구가-하는-일)
2. [처음 한 번만 하는 설정](#2-처음-한-번만-하는-설정)
3. [매일 아침 리포트 읽는 법](#3-매일-아침-리포트-읽는-법)
4. [숫자 세 개 — σ, z, 분위](#4-숫자-세-개--σ-z-분위)
5. [지표 사전](#5-지표-사전)
6. [고장났을 때](#6-고장났을-때)
7. [지표 추가·수정하기](#7-지표추가수정하기)
8. [파일이 뭐가 뭔지](#8-파일이-뭐가-뭔지)
9. [10분 루틴](#9-10분-루틴)

---

## 1. 이 도구가 하는 일

매일 아침 6시 30분, GitHub 서버가 알아서:

```
75개 지표 수집  →  표준화  →  규칙 판정  →  리포트 3종 생성
(FRED·Yahoo·ECOS)   (σ·z·분위)   (경보·괴리)    (읽기·대시보드·데이터)
```

노트북이 꺼져 있어도 돌아간다. 결과는 GitHub에 저장되므로 폰이든 학교 컴퓨터든
로그인만 하면 볼 수 있다.

### 왜 만들었나

수기로 "달러 소폭 하락, HY 소폭 하락" 식으로 적으면 세 가지 문제가 생긴다.

| 문제 | 도구의 해결 |
|---|---|
| "소폭"이 얼마인지 모른다 | 모든 변동을 **σ 단위**로 환산. 2σ 미만은 노이즈로 분류 |
| 레벨이 낮으면 판단을 포기하게 된다 | **백분위**로 상대 위치를 표시 |
| 인과를 상상으로 채우게 된다 | 인과 서술 대신 **지표 간 괴리**만 기계적으로 검출 |

도구는 **판단하지 않는다.** "무엇이 평소와 다른가"까지만 알려주고,
"왜 그런가"와 "그래서 뭘 할 것인가"는 사람이 7번 항목에 쓴다.

---

## 2. 처음 한 번만 하는 설정

### 2-1. API 키 발급

| 키 | 어디서 | 없으면 |
|---|---|---|
| `FRED_API_KEY` | [fred.stlouisfed.org/docs/api/api_key.html](https://fred.stlouisfed.org/docs/api/api_key.html) | 미국 금리·크레딧·유동성 **전부 누락** |
| `ECOS_API_KEY` | [ecos.bok.or.kr](https://ecos.bok.or.kr) → 오픈API → 인증키 신청 | 한국 금리 5종 누락 |

둘 다 무료, 즉시 발급된다.

### 2-2. GitHub에 등록

리포지토리 → **Settings** → 왼쪽 **Secrets and variables** → **Actions**
→ **New repository secret**

| Name | Secret |
|---|---|
| `FRED_API_KEY` | 발급받은 키 |
| `ECOS_API_KEY` | 발급받은 키 |

**주의:** 이름은 대문자·언더바 그대로. 값에 따옴표를 붙이지 않는다.
복사할 때 앞뒤 공백이 묻어오지 않게 한다.

### 2-3. 쓰기 권한 켜기

**Settings** → **Actions** → **General** → 맨 아래 **Workflow permissions**
→ **Read and write permissions** → **Save**

이걸 안 하면 리포트는 만들어지는데 저장 단계에서 403 에러가 난다. 가장 흔한 실패다.

### 2-4. 첫 실행

**Actions** 탭 → 왼쪽 `macro-daily` → 우측 **Run workflow** → 초록 버튼

1~2분 뒤 초록 체크가 뜨면 성공이다. `output/` 폴더에 파일이 생겼는지 확인한다.

### 2-5. 정상 작동 체크리스트

새 리포트를 열어 이 세 가지만 본다.

- [ ] 맨 위 **0번 섹션 커버리지가 90% 이상**
- [ ] **순유동성이 5,000~6,000 범위의 양수** (음수 90만이면 단위 버그)
- [ ] 금리 1D가 **`+0.00bp`가 아닌 실제 값**

셋 다 통과하면 끝이다.

---

## 3. 매일 아침 리포트 읽는 법

리포트는 7개 섹션이다. **위에서부터 순서대로** 읽도록 설계했다.

### 0번 · 수집 상태 — 반드시 먼저 본다

```
0. 수집 상태 — 70/75종 (커버리지 93%)
- yahoo 결손 2종: VIX 3개월, USD/CNH
- 비정상 지연 0종
- 정상 시차 65종 (미국장 마감·주간 발표에 따른 구조적 지연)
```

**왜 첫 번째인가:** 지표가 조용히 빠지는 게 가장 위험하다. HY 스프레드가 누락된 리포트를
보고 "크레딧 조용하네"라고 결론 내리면 정반대로 틀린다.

| 커버리지 | 판단 |
|---|---|
| 95% 이상 | 정상 |
| 90~95% | 어떤 지표가 빠졌는지 확인 후 읽기 |
| 90% 미만 | 리스크 스코어 신뢰 불가. 6번 트러블슈팅으로 |

**"정상 시차"는 고장이 아니다.** 06:30 KST에 실행하니 미국 데이터는 원래 하루 이틀 전
것이 최신이다. 주간 지표는 6일 전이 최신이어도 정상이다. **"비정상 지연"에만** 반응한다.

### 1번 · 오늘 움직인 것

```
1. 오늘 유의미하게 움직인 지표 (|일간변동| ≥ 2σ)
- MOVE(채권 변동성) 77.92 (+9.93%, +2.1σ, 26분위)
```

여기 나온 것만 오늘의 뉴스다. **"없음"이면 아무 일도 없었던 것**이고,
그럴 땐 1번을 건너뛰고 2·3번으로 간다.

### 2번 · 레벨 경보

절대 수준이 미리 정한 선을 넘은 지표다. 변화가 없어도 뜬다.

```
[경보] 미국 CCC OAS = 10.230% (주의 8.000% · 경보 10.000%)
[주의] 지급준비금 = 2,993 (주의 3,000 · 경보 2,800)
```

**1번과 2번의 차이:** 1번은 "오늘 갑자기 움직였다", 2번은 "위험한 곳에 와 있다".
서서히 나빠지는 것은 1번에 안 잡히고 2번에만 잡힌다.

### 3번 · 정합성 위반 (괴리)

**실질적으로 가장 값어치 있는 섹션이다.**

```
구리/금 -5.29% vs 미국 10Y +2.62% (20일, 격차 7.91%p)
— 금리 상승이 성장이 아닌 공급/재정 요인일 가능성
```

같이 움직여야 할 두 지표가 갈라진 상황이다. 둘 중 하나는 틀렸다는 뜻이고,
어느 쪽이 틀렸는지 밝히는 게 리서치다.

### 4번 · 전체 패널

75개 전부. 매일 다 읽을 필요 없다. **1~3번에서 이름이 나온 지표의 맥락을 확인할 때** 본다.

### 5번 · 리스크 스코어 기여도

스코어가 왜 그 값이 됐는지 분해한다.

```
미국 HY OAS: z -0.96 × w +0.20 = -0.191
미국 CCC OAS: z +1.91 × w +0.10 = +0.191
```

읽는 법: `z`는 그 지표의 현재 위치, `w`는 미리 정한 가중치, 곱이 기여도다.
**부호가 반대인 항목이 섞여 있으면** 시장이 한 방향으로 정렬되지 않았다는 뜻이다.

### 6번 · 수동 확인 항목

자동 수집이 안 되는 것들. 유료 단말이 필요하거나 웹에 흩어져 있다.

### 7번 · 해석 — 여기가 본체다

**도구가 하는 일은 6번까지다.** 7번은 비어 있고, 이걸 채우는 게 실제 작업이다.

| 항목 | 쓰는 법 |
|---|---|
| **가설** | 오늘 데이터를 한 문장으로. "명목금리 상승이 전부 실질금리 몫이다" |
| **반증 조건** | 이 가설이 틀렸다면 무엇이 관찰될까. "BEI가 10bp 이상 오르면 폐기" |
| **포지션 함의** | 내 포트폴리오에 뭘 의미하나 |
| **내일 확인할 것** | 구체적 지표 이름 |

**반증 조건이 핵심이다.** 이걸 미리 안 쓰면 나중에 데이터를 자기 가설에 맞춰
읽게 된다. 매일 쓴 반증 조건을 한 달 뒤 돌아보면 자기 판단의 적중률을 알 수 있다.

---

## 4. 숫자 세 개 — σ, z, 분위

리포트 전체가 이 셋으로 돌아간다. 이것만 이해하면 나머지는 사전이다.

### σ (시그마) — "오늘 움직임이 평소보다 큰가"

**계산:** 오늘 변동 ÷ 최근 20일 변동의 표준편차

**비유:** 매일 5분씩 지각하던 사람이 오늘 7분 지각한 건 뉴스가 아니다.
40분 지각했으면 뉴스다. σ는 이 판단을 자동화한다.

| σ | 의미 |
|---|---|
| ±1σ 이내 | 완전히 평범. 68%의 날이 여기 |
| ±2σ | 20일에 하루 있을까 말까. **여기부터 본다** |
| ±3σ 이상 | 사건. 이유를 찾아야 한다 |

리포트 1번 섹션이 자동으로 2σ 이상만 뽑아준다.

### z (z-score) — "지금 위치가 평소와 다른가"

**계산:** (현재값 − 1년 평균) ÷ 1년 표준편차

σ가 **오늘의 움직임**이라면 z는 **현재 서 있는 자리**다.

- z = 0 → 1년 평균과 같은 자리
- z = +2 → 1년 평균보다 뚜렷하게 높은 자리
- z = −2 → 뚜렷하게 낮은 자리

**σ와 z는 독립이다.** 매일 조금씩 올라 아주 높은 곳에 도달했다면
σ는 계속 0에 가깝고 z만 커진다. 오늘 리포트의 미국 30Y가 그렇다(σ −0.2, z +2.5).

### 분위 (백분위) — "3년 중 어디쯤인가"

**계산:** 최근 3년 중 지금보다 낮았던 날의 비율

z보다 직관적이고, 극단값에 덜 흔들린다.

| 분위 | 의미 |
|---|---|
| 50 | 3년 중간값 |
| 90 | 3년 중 상위 10% |
| 10 | 3년 중 하위 10% |

**HY 2.77%를 두고 "낮아서 의미 없다"고 하던 문제가 여기서 풀린다.**
레벨이 낮아도 분위가 85면 "**낮지만 3년 내 상대적으로는 올라온**" 상태이고,
이건 정보다.

### 셋을 함께 읽기

| σ | z | 분위 | 상태 |
|---|---|---|---|
| 0 | +2.5 | 99 | 조용히 극단으로 올라와 있다 — **가장 놓치기 쉬운 위험** |
| +3 | 0 | 50 | 오늘 크게 튀었지만 위치는 평범 — 일시적 충격 |
| +3 | +2.5 | 99 | 높은 자리에서 또 튀었다 — **즉시 확인** |
| 0 | 0 | 50 | 아무 일 없음 |

---

## 5. 지표 사전

### 금리 — 돈의 가격

| 지표 | 쉽게 말하면 |
|---|---|
| 2Y | 앞으로 2년간 연준이 뭘 할지에 대한 시장 베팅 |
| 10Y | 장기 성장 + 물가 + 재정위험이 섞인 값 |
| **2s10s** | 10Y − 2Y. 마이너스면 "장기가 단기보다 싸다"는 비정상 |
| **3m10y** | 같은 개념, 3개월 기준. **연준이 실제로 보는 건 이쪽** |
| 5s30s | 초장기 기울기. 벌어지면 재정적자·국채발행 부담 |

**가장 중요한 분해:**

```
명목 10Y  =  실질금리(DFII10)  +  기대인플레(BEI)
```

10Y가 올랐을 때 이 분해가 없으면 해석이 불가능하다.

| 무엇이 올렸나 | 의미 | 영향 |
|---|---|---|
| 실질금리 | 경기 호조 또는 연준 매파 | 주식·금에 악재 |
| 기대인플레 | 물가 우려 | 금에 호재, 채권에 악재 |

**5y5y 선도 기대인플레:** "5년 뒤부터 5년간의 물가 기대". 단기 유가에 안 흔들려서
**연준이 장기 신뢰도를 재는 지표**다. 2.5% 위로 굳으면 연준이 긴장한다.

### 크레딧 — 회사가 돈 빌릴 때 얹는 가산금리

국채보다 얼마나 더 얹어야 회사가 돈을 빌리나. 벌어지면 시장이 부도를 걱정하는 것.

| 지표 | 쉽게 말하면 |
|---|---|
| IG OAS | 우량 기업 |
| HY OAS | 정크등급 기업 |
| **CCC OAS** | 정크 중에서도 최하위. **가장 먼저 부러지는 곳** |
| HY/IG 배율 | 정크가 우량 대비 몇 배 비싼가 |

**CCC를 따로 보는 이유:** 사이클이 꺾일 때 HY 전체는 조용한데 CCC만 먼저 벌어진다.
통상 2~6주 선행한다. HY 하나만 보면 이 시차를 통째로 놓친다.

**배율을 보는 이유:** HY와 IG가 같이 조금씩 벌어지면 그냥 금리 영향이다.
HY만 벌어져 배율이 커지면 **등급별 차별화가 시작된 것**이고, 이게 진짜 신호다.

### 유동성 — 시장에 떠다니는 돈

| 지표 | 쉽게 말하면 |
|---|---|
| 연준 자산 | 연준이 뿌린 돈 총량 |
| TGA | 정부 통장 잔고. **여기 쌓이면 시중에서 빠진 것** |
| RRP | 머니마켓펀드가 연준에 맡긴 돈. 역시 잠긴 돈 |
| **순유동성** | 연준자산 − TGA − RRP = **실제로 돌아다니는 돈** |
| 지급준비금 | 은행 여유자금. 3.0조 달러 아래면 레포 사고 위험 |
| SOFR − EFFR | 은행 간 초단기 자금이 뻑뻑한지 재는 온도계 |
| NFCI | 100여 지표를 합친 종합 긴축도. 0 위 = 평균보다 빡빡 |

"지수가 올랐다"에 **자금원**을 붙일 수 있는 게 순유동성이다. 늘면서 오른 것과
줄어드는데 오른 것은 지속성이 전혀 다르다.

**SOFR−EFFR 주의:** 평소 0에 붙어 있다가 자금시장이 막히면 튄다.
단, **분기말·월말엔 계절적으로 튄다.** 분기 중간에 벌어질 때만 의미가 있다.

### 변동성 — 얼마나 겁먹었나

| 지표 | 쉽게 말하면 |
|---|---|
| VIX | 향후 30일 주식 변동 예상치 |
| **VIX3M/VIX** | **기간구조.** 스팟-선물 레벨 비교보다 이게 신호 |
| MOVE | 채권판 VIX. 금리가 얼마나 예측 불가능한가 |
| SKEW | 폭락 대비 보험료. 높으면 큰손이 꼬리위험을 사는 중 |

**기간구조 읽는 법:**

| 상태 | 비율 | 의미 |
|---|---|---|
| 콘탱고 | > 1 | 정상. 먼 미래가 더 불확실 |
| **백워데이션** | **< 1** | **지금 당장이 제일 무섭다** — 실질적 스트레스 |

VIX 선물 19.15 > 현물 17.83은 그냥 콘탱고다. **1.0을 깨는 순간**이 신호다.

**MOVE 오해 주의:** 채권 '지수'가 아니라 채권 옵션 내재변동성이다.
금리 방향이 아니라 **금리 불확실성**을 잰다. 70대는 매우 조용하다는 뜻이지,
자금이 주식으로 옮겨갔다는 근거가 아니다.

**주식-채권 60일 상관 — 포트폴리오 운용자에겐 필수:**

| 부호 | 의미 |
|---|---|
| 음수 | 채권이 주식 하락을 방어. 전통적 60/40 작동 |
| **양수** | **둘이 같이 떨어진다.** 2022년이 그랬다 |

플러스로 굳으면 포트폴리오 구조 자체를 재검토해야 한다.

### 주식 내부 — 지수 뒤에 뭐가 있나

| 지표 | 쉽게 말하면 | 낮아지면 |
|---|---|---|
| **RSP/SPY** | 동일가중 ÷ 시총가중 | 소수 대형주만 끌고 가는 중 |
| SMH/SPY | 반도체 상대강도 | "AI 머니 복귀"의 **유일한 검증 수단** |
| XLY/XLP | 경기소비재 vs 필수소비재 | 소비 둔화 |
| XLK/XLU | 기술 vs 유틸리티 | 위험선호 후퇴 |
| IWM/SPY | 소형주 상대강도 | 금리·신용 여건 악화 |

**RSP/SPY 쉽게:** 500개에 똑같이 나눠 담은 펀드와 시총 비중대로 담은 펀드의 비교다.
후자가 훨씬 좋으면 상위 몇 종목만 오르고 있다는 뜻이다.
**지수는 신고가인데 이 비율이 떨어지면 상승이 취약하다.**

### 외환

| 지표 | 쉽게 말하면 |
|---|---|
| DXY | 6개 통화 대비 달러. **유로 비중 57.6%라 사실상 유로 반대매매** |
| 광의 무역가중 달러 | 26개 통화 반영. **진짜 달러 강약은 이쪽** |
| **USD/CNH** | 역외 위안. **원화의 실제 앵커** |
| USD/JPY | 급락하면 엔캐리 청산 → 전 세계 동시 매도 |

원화 움직임은 DXY보다 **CNH 대비 움직임**의 설명력이 높다.
원화가 위안화와 따로 놀 때만 국내 요인(수급·정책·신용)을 얘기하면 된다.

### 원자재

| 지표 | 쉽게 말하면 |
|---|---|
| **브렌트 − WTI** | 벌어지면 비미국 지역 공급 차질·지정학 |
| **구리/금** | 산업금속 ÷ 안전자산 = **성장 기대 대리변수** |
| 금/은 배율 | 높으면 안전자산 선호, 낮으면 산업수요 강함 |

**구리/금이 유용한 이유:** 구리는 경기가 좋아야 오르고 금은 불안해야 오른다.
그래서 이 비율은 성장 기대를 나타내고, 정상적으로는 10Y와 같은 방향으로 간다.
갈라지면 금리 상승이 성장이 아니라 **공급(국채발행)·재정 우려** 때문일 가능성이 크다.

**XLE는 원자재가 아니라 에너지 '주식'이다.** 유가가 오르는데 XLE가 안 따라오면
시장이 그 상승을 일시적으로 보거나 기업 마진에 문제가 있다고 보는 것이다.

### 한국

| 지표 | 쉽게 말하면 |
|---|---|
| 회사채(AA-) − 국고3Y | 한국 기업의 **실제 조달 비용** 프리미엄 |
| **국고3Y − 기준금리** | 마이너스면 인하 기대 선반영, 플러스면 인상 기대 |
| EWY/KOSPI | 달러 기준 ÷ 원화 기준 = **순수 환율 효과** |

CDS는 외국인이 보는 **국가 신용**, 회사채 스프레드는 국내 기업의 **실제 조달 여건**이다.
후자가 국내 신용 사이클에 훨씬 민감하다.

**국고3Y − 기준금리:** 이 값이 −0.5%p면 시장이 이미 두 번의 인하를 가격에 넣은 것이다.
인하가 실제로 와도 채권은 안 오르고, **기대가 꺾이면 되돌림이 나온다.**
"금리 인하 = 채권 강세"로 단순화하면 틀린다.

### 컴포짓 리스크 스코어

| 값 | 상태 |
|---|---|
| +1.0 이상 | 리스크오프 |
| +0.3 ~ +1.0 | 경계 |
| −0.3 ~ +0.3 | 중립 |
| −1.0 이하 | 리스크온 |

**절대 수준보다 전일 대비 이동 방향이 정보량이 크다.**

> **주의:** 가중치는 사후검증되지 않은 임의값이다. `output/panel.csv`로
> 직접 예측력을 확인한 뒤 조정할 것. 그전까지는 참고 지표로만 쓴다.

---

## 6. 고장났을 때

### 증상별 대처

| 증상 | 원인 | 조치 |
|---|---|---|
| 금리·크레딧·유동성 그룹 통째 실종 | FRED 실패 | `FRED_API_KEY` 등록 확인 |
| 한국 금리만 없음 | ECOS 키 미설정 | `ECOS_API_KEY` 등록 |
| 1D가 전부 `+0.00%` | 구버전 (ffill 버그) | v1.2 이상으로 갱신 |
| 순유동성이 음수 90만 | 단위 불일치 | v1.2 이상으로 갱신 |
| 실업수당 청구 없음 | 토요일 날짜 탈락 | v1.3 이상으로 갱신 |
| Actions에서 403 에러 | 쓰기 권한 없음 | Settings → Actions → Read and write |
| `Node.js 20 deprecated` | 구버전 액션 | checkout@v5 / setup-python@v6 / upload-artifact@v5 |
| VIX3M·MOVE 등 간헐 누락 | Yahoo 응답 불안정 | v1.3의 대체 티커 폴백. 계속되면 티커 추가 |

### 로그 보는 법

**Actions** 탭 → 실패한 실행 클릭 → **build** → **리포트 생성** 펼치기

경고 형식:

```
[fred] HY_OAS(BAMLH0A0HYM2) 실패: HTTP 403
[yahoo] VIX3M 전 티커 실패: ^VIX3M|VIX3M.INDX|^VXV
[ecos] KTB3Y(817Y002/D/010200000) 실패: ... 통계표/항목코드 재확인 필요
```

대괄호 안이 소스, 괄호 안이 코드, 뒤가 원인이다.

### 데이터가 이상할 때

**숫자가 틀린 것 같으면 반드시 원본과 대조한다.** 도구가 티커를 잘못 잡거나
소스가 오류를 낼 수 있다.

| 대상 | 대조처 |
|---|---|
| 미국 금리·스프레드 | fred.stlouisfed.org |
| 미국 주식·ETF | finance.yahoo.com |
| KOSPI·KOSDAQ | KRX 정보데이터시스템, 네이버금융 |
| 한국 금리 | ECOS 통계검색 |

**교차 검증 요령:** 같이 움직여야 할 것끼리 비교한다.
KOSPI가 +7%인데 KOSDAQ이 +1%, EWY가 +5%면 셋 중 하나가 틀렸다.

### 오프라인 검증

로컬에 파이썬이 있다면, 네트워크 없이 파이프라인만 점검할 수 있다.

```bash
python -m ludis.cli --offline
```

이게 통과하는데 실제 실행이 실패하면 **코드가 아니라 네트워크·API 문제**다.
원인을 좁히는 데 유용하다.

---

## 7. 지표 추가·수정하기

**`config/indicators.yaml` 하나만 고치면 된다.** 파이썬 코드는 건드리지 않는다.

### 일반 지표 추가

```yaml
- {id: TIPS5Y, label: 미국 5Y 실질금리, source: fred, code: DFII5,
   unit: pct, risk_dir: up, thresholds: {warn: 2.0, alert: 2.5},
   note: "왜 넣었는지 여기 쓰면 리포트에 함께 나온다"}
```

| 필드 | 설명 |
|---|---|
| `id` | 영문 대문자·언더바. 파생지표 계산식에서 이 이름을 쓴다 |
| `label` | 리포트에 표시될 한글 이름. **콤마가 들어가면 따옴표로 감쌀 것** |
| `source` | `fred` / `yahoo` / `ecos` |
| `code` | 소스별 코드. Yahoo는 `A\|B\|C`로 대체 티커 나열 가능 |
| `unit` | `pct`(금리·%) / `bp` / `idx` / `usd` / `ratio` / `bn` / `krw` |
| `risk_dir` | `up`(오르면 위험) / `down`(내리면 위험) / `none` |
| `thresholds` | 경보선. 생략 가능 |
| `freq` | `daily`(기본) / `weekly` / `monthly`. **지연 판정 기준** |
| `scale` | 원단위 보정 계수. FRED 백만달러 계열은 `0.001` |

### 파생지표 추가

같은 그룹의 `derived:` 아래에. 지표 id와 사칙연산만 쓸 수 있다.

```yaml
derived:
  - {id: TIPS_CURVE, label: 5s10s 실질, expr: "REAL10Y - TIPS5Y",
     unit: pct, risk_dir: none}
```

### 괴리 규칙 추가

```yaml
divergences:
  - {a: SMH, b: NDX, lookback: 20, min_gap: 5.0, mode: sign,
     msg: "반도체가 나스닥과 이탈 — AI 테마 내부 균열"}
```

| mode | 발동 조건 |
|---|---|
| `sign` | 부호가 엇갈리고 격차가 `min_gap`(%p) 이상 |
| `gap` | 부호 무관, 격차만 |
| `same_sign` | **반대로 움직여야 할 둘이 같은 방향으로** 움직일 때 |

### 임계값 조정

경보가 너무 자주 뜨거나 안 뜨면 `thresholds`를 고친다.
**분위를 참고하는 게 좋다** — 대략 90분위쯤에 `warn`, 97분위쯤에 `alert`를 두면
한 달에 몇 번 정도 발동한다.

### 수정 후 확인

`config/indicators.yaml`을 GitHub에서 편집하고 Commit → **Actions → Run workflow**.
YAML은 들여쓰기가 문법이므로, 편집할 때 **메모장 말고 VS Code**를 쓴다.

---

## 8. 파일이 뭐가 뭔지

```
ludis-macro-monitor/
├── config/
│   └── indicators.yaml      ← 지표 정의. 평소 여기만 고친다
├── ludis/
│   ├── sources.py           수집 (FRED/Yahoo/ECOS)
│   ├── pipeline.py          계산 (σ·z·분위·괴리·스코어)
│   ├── render.py            출력 (Markdown/JSON/HTML)
│   └── cli.py               실행 진입점
├── .github/workflows/
│   └── daily.yml            자동 실행 스케줄
└── output/                  ← 결과물
    ├── YYYY-MM-DD.md        읽는 리포트 (노션 붙여넣기용)
    ├── index.html           대시보드 (브라우저)
    ├── latest.json          대시보드 프론트엔드용
    └── panel.csv            원계열 (백테스트용, git 미포함 · 아티팩트로 제공)
```

**`panel.csv` 주의:** 용량이 3MB를 넘어 리포지토리에는 커밋하지 않는다(`.gitignore` 처리).
매 실행마다 **Actions 아티팩트로 30일간 보관**되므로, 필요할 때
Actions 탭 → 해당 실행 → 하단 Artifacts 에서 내려받는다.
로컬에 파이썬이 있다면 `python -m ludis.cli --save-panel` 로 언제든 다시 만들 수 있다
(원본 소스에서 전 기간을 다시 받으므로 과거 데이터도 그대로 복원된다).

---

## 9. 10분 루틴

| 시간 | 할 일 |
|---|---|
| 1분 | **0번** 커버리지 확인. 90% 미만이면 6번 트러블슈팅 |
| 2분 | **1·2·3번**만 읽는다. 여기 없으면 오늘은 조용한 날 |
| 3분 | 1~3번에 나온 지표를 **4번에서 찾아** 분위·z 확인 |
| 4분 | **7번 작성.** 가설 한 줄, 반증 조건 한 줄 |

**4번을 매일 다 읽지 않는다.** 75개를 전부 훑으면 지쳐서 사흘 만에 그만두게 된다.
1~3번이 걸러주는 게 이 도구의 존재 이유다.

### 주 1회 (금요일 권장)

- 이번 주 7번에 쓴 **반증 조건이 충족됐는지** 점검
- 리스크 스코어 5일 추이
- `panel.csv`를 받아 관심 지표 그래프 확인

### 월 1회

- 자주 뜨거나 전혀 안 뜨는 경보의 `thresholds` 조정
- 한 번도 안 쓴 지표는 제거 검토 (많다고 좋은 게 아니다)
- 컴포짓 가중치를 `panel.csv`로 검증

---

## 마지막으로

이 도구는 **판단을 대신하지 않는다.** "무엇이 평소와 다른가"까지가 기계의 몫이고,
"왜 그런가"와 "그래서 무엇을 할 것인가"는 사람의 몫이다.

7번 항목을 매일 채우는 것 — 그것만이 실제로 실력이 되는 부분이다.
1~6번은 그 4분을 확보하기 위한 장치일 뿐이다.
